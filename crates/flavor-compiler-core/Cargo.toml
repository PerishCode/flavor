[package]
name = "flavor-compiler-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Compiler substrate primitives for flavor syntax frontends."

[dependencies]
rowan = "0.16"
serde = { version = "1", features = ["derive"] }
