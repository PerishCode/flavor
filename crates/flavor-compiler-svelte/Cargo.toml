[package]
name = "flavor-compiler-svelte"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "AST-only Svelte compiler substrate for flavor."

[dependencies]
flavor-compiler-core = { path = "../flavor-compiler-core" }
flavor-compiler-ts = { path = "../flavor-compiler-ts" }
