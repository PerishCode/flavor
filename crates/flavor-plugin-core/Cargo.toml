[package]
name = "flavor-plugin-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Plugin substrate primitives for flavor syntax products."

[dependencies]
rowan = "0.16"
serde = { version = "1", features = ["derive"] }
