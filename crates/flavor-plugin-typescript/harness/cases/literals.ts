export const count = 1_024n;
export const mask = 0b1010_1010;
export const hex = 0xFF_FFn;
export const ratio = left / right;
export const pattern = /foo\/[a-z]+/gi;
export const label = `user:${profile.name}-${items.length}`;
export const nested = `outer ${items.map((item) => `${item.id}`).join(",")}`;
